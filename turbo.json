{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "test": {
      "inputs": ["*.go"],
      "outputs": []
    },
    "test:backend": {
      "inputs": ["*.go"],
      "outputs": []
    }
  }
}
